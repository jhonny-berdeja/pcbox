---
- name: Crear carpeta en el servidor
  hosts: pcbox-server-prod
  become: true
  gather_facts: false

  tasks:
    - name: Eliminar /opt/hello
      ansible.builtin.file:
        path: /opt/hello
        state: absent
